# P0-T4 Execution Plan

- Starting commit: `7759bf486e32e866d310dd02ee493ae849235b89`.
- Change P0-T4 from `todo` to `in_progress` before fixture implementation.
- Lock current exact stable React/Vite dependencies and rerun the full dependency/license policy.
- Create one private React + Vite + TypeScript workspace package without network or product integration.
- Represent every malicious case with stable IDs and a closed category union.
- Include fake password/form values, private subtree, user private selector, sensitive path/query/fragment, prompt injection, secret-shaped diagnostic/error text, and oversized Unicode/control content.
- Render injection payloads only as React text and prove markup escaping with server-side smoke tests.
- Run a real Vite production build and verify bounded local output exists.
- Run build, typecheck, lint, Vitest, roadmap/workspace/license, offline clean install, 98 preflight regressions and Git whitespace checks.
- Record bounded command/results and SHA-256 evidence.
- Mark P0-T4 done, add a concise completion summary below its prompt, remove the checkpoint and commit atomically.
