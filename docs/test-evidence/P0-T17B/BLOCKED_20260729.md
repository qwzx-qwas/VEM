# P0-T17B external-execution blocker

- Task: `P0-T17B`
- Date: `2026-07-29`
- State: `blocked`
- Decision: `P0-VALUE=pending`
- Active preregistration:
  `20260729T171610+0800/`
- Preregistration SHA-256:
  `2bf456e2220cf61fb78c97c1854d6575847d783b66404ba3db3746380573b1bb`
- Bound runner SHA-256:
  `f5d58e279c38df73943c2decb56ebdf958cd487a6798cf790c0224a29fe172c3`

## Reproducible state

- The plan fixes five tasks and counterbalances arm order as
  D/V, V/D, D/V, V/D, D/V.
- Every arm requires a distinct `codex exec --ephemeral` context under a
  read-only sandbox.
- The external participant payload is repo-derived: the two frozen fixture
  source files, one frozen task prompt, and, only for the assisted arm, one
  frozen VEM context.
- Evaluator-only ground truth and later holdout records are never copied to a
  participant workspace.
- `20260729T171610+0800/results/` does not exist. No arm was started, no
  participant payload was sent, and no result was observed.

## Blocking event

The bounded external execution request was rejected before process creation
because sending the repo-derived participant payload to repeated external
Codex/model runs requires explicit informed user authorization. Repeating or
indirectly recreating those external calls without that authorization is not
permitted.

## Resume condition

The owner must explicitly authorize sending the described participant payload
for ten ephemeral read-only Codex/model runs. On resume, the runner must first
verify both hashes above and fail closed if either changed. No dependent task
may begin unless the completed pilot records an honest `P0-VALUE=continue`.
