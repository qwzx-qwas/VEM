# P0-T17B preregistration attempt — abandoned before results

This preregistration was frozen with hash
`6d668282174baa944246ad76445ed51ac091a65e5e02b85b7f1c9982473b69fd`,
but no pilot arm was started and no result was observed.

The attempt bound the runner correctly, but the exported participant fixture
also contained repository test files. Root Vitest consequently discovered
the evidence copies as suites outside their workspace package context. The
immutable directory is retained unchanged; the replacement preregistration
exports only the two runtime source files needed by both arms.
